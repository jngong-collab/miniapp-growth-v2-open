/* pages/profile/profile.wxss — 现代东方会员中心 */

.page-profile {
  min-height: 100vh;
  background: var(--bg-page);
  position: relative;
  overflow-x: hidden;
  padding-bottom: env(safe-area-inset-bottom);
}

.profile-header-bg {
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 480rpx;
  background: linear-gradient(180deg, var(--color-primary-bg) 0%, var(--bg-page) 100%);
  z-index: 0;
}

/* ====== 会员卡 ====== */
.member-card-wrap {
  position: relative;
  z-index: 2;
  padding: var(--spacing-md) var(--spacing-lg) 0;
}

.member-card {
  position: relative;
  width: 100%;
  border-radius: var(--radius-xl);
  overflow: hidden;
  background: linear-gradient(135deg, #D4A574, #B8916B, #8B6B4A);
  color: #fff;
  box-shadow: 0 20rpx 48rpx rgba(184,145,107,0.18);
}

.card-shine {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: repeating-linear-gradient(
    135deg,
    transparent, transparent 18px,
    rgba(255,255,255,0.04) 18px, rgba(255,255,255,0.04) 36px
  );
  pointer-events: none;
  z-index: 0;
}

.card-shine::after {
  content: '';
  position: absolute;
  top: -60%; left: -30%;
  width: 160%; height: 160%;
  background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.14), transparent 45%);
  pointer-events: none;
}

.card-content {
  position: relative;
  z-index: 1;
  padding: var(--spacing-lg);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xl);
}

.user-info { display: flex; align-items: center; }
.avatar {
  width: 88rpx; height: 88rpx;
  border-radius: 50%;
  border: 2rpx solid rgba(255,255,255,0.4);
  overflow: hidden;
  background: rgba(255,255,255,0.2);
  display: flex; align-items: center; justify-content: center;
  font-size: var(--font-xl);
  flex-shrink: 0;
}

.user-meta { margin-left: var(--spacing-md); display: flex; flex-direction: column; gap: var(--spacing-xs); }
.user-name {
  font-size: var(--font-md); font-weight: 700;
  letter-spacing: 0.5rpx;
  text-shadow: 0 2rpx 6rpx rgba(0,0,0,0.12);
}

.level-badge {
  display: inline-flex; align-items: center;
  background: rgba(255,255,255,0.2);
  backdrop-filter: blur(8px);
  padding: 4rpx 12rpx;
  border-radius: var(--radius-capsule);
  font-size: var(--font-xs); font-weight: 600;
  border: 1rpx solid rgba(255,255,255,0.3);
  align-self: flex-start;
}

.badge-icon { margin-right: 4rpx; font-size: var(--font-xs); }

.card-row-bottom {
  display: flex; justify-content: space-between; align-items: flex-end;
}

.balance-info { display: flex; flex-direction: column; }
.balance-label { font-size: var(--font-xs); opacity: 0.85; margin-bottom: 4rpx; }
.balance-amount {
  font-size: 44rpx; font-weight: 800; line-height: 1;
  text-shadow: 0 3rpx 10rpx rgba(0,0,0,0.12);
  letter-spacing: -1rpx;
}

.card-brand {
  font-size: var(--font-sm); font-weight: 800;
  letter-spacing: 4rpx;
  color: rgba(255,255,255,0.25);
  text-transform: uppercase;
}

/* ====== 统计条 ====== */
.stat-bar {
  position: relative; z-index: 3;
  margin: -24rpx var(--spacing-lg) var(--spacing-md);
  display: flex; align-items: center;
  padding: var(--spacing-md) 0;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  border: 1rpx solid var(--color-border);
}

.stat-cell {
  flex: 1; display: flex; flex-direction: column; align-items: center;
  transition: transform 0.2s ease;
}

.stat-cell:active { transform: scale(0.95); opacity: 0.8; }

.stat-val {
  font-size: var(--font-lg); font-weight: 800; margin-bottom: 6rpx;
  color: var(--color-text-title);
}

.stat-val.stat-eco { color: var(--color-eco-dark); }
.stat-val.stat-gold { color: var(--color-gold); }

.stat-label { font-size: var(--font-xs); color: var(--color-text-sub); font-weight: 500; }

.stat-divider {
  width: 1rpx; height: 40rpx;
  background: linear-gradient(to bottom, transparent, var(--color-border), transparent);
}

/* ====== 主体 ====== */
.profile-body { padding: 0 var(--spacing-lg); position: relative; z-index: 2; }

.section-block {
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
  margin-bottom: var(--spacing-md);
  padding: var(--spacing-md);
  overflow: hidden;
  border: 1rpx solid var(--color-border);
}

.section-head {
  display: flex; justify-content: space-between; align-items: center;
  margin-bottom: var(--spacing-md);
}

.section-name,
.section-title { font-size: var(--font-md); font-weight: 700; color: var(--color-text-title); }
.section-more { font-size: var(--font-sm); color: var(--color-text-sub); font-weight: 500; }

.profile-editor {
  display: flex;
  gap: var(--spacing-md);
  align-items: stretch;
}

.profile-avatar-picker {
  width: 188rpx;
  min-height: 220rpx;
  border-radius: var(--radius-lg);
  background: linear-gradient(180deg, #FBF6EF, #F5E8D8);
  border: 1rpx solid rgba(139,107,74,0.12);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-sm);
  box-sizing: border-box;
}

.profile-avatar-picker::after,
.profile-save-btn::after {
  border: none;
}

.profile-avatar-image,
.profile-avatar-fallback {
  width: 108rpx;
  height: 108rpx;
  border-radius: 50%;
}

.profile-avatar-image {
  display: block;
  overflow: hidden;
}

.profile-avatar-fallback {
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255,255,255,0.75);
  font-size: 48rpx;
}

.profile-avatar-tip {
  font-size: var(--font-xs);
  color: #8B6B4A;
  font-weight: 600;
  line-height: 1.5;
}

.profile-editor-form {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.profile-name-input {
  min-height: 88rpx;
  padding: 0 24rpx;
  border-radius: var(--radius-lg);
  background: #fff;
  border: 1rpx solid var(--color-border);
  font-size: var(--font-base);
  color: var(--color-text-title);
  box-sizing: border-box;
}

.profile-editor-desc {
  font-size: var(--font-xs);
  color: var(--color-text-sub);
  line-height: 1.7;
}

.profile-save-btn {
  margin: 0;
  height: 78rpx;
  border-radius: var(--radius-capsule);
  background: linear-gradient(135deg, #D4A574, #B8916B);
  color: #fff;
  font-size: var(--font-sm);
  font-weight: 700;
  box-shadow: 0 10rpx 24rpx rgba(184,145,107,0.14);
}

.profile-save-btn[disabled] {
  background: #E7D7C5;
  color: rgba(80,54,34,0.66);
  box-shadow: none;
}

/* 订单快捷入口 */
.order-grid { display: flex; justify-content: space-around; }
.order-entry {
  display: flex; flex-direction: column; align-items: center;
  padding: var(--spacing-xs) 0;
  transition: transform 0.2s ease;
}

.order-entry:active { transform: scale(0.9); opacity: 0.8; }

.entry-icon-wrap {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: var(--spacing-sm);
}

.entry-icon { font-size: 40rpx; }
.entry-text { font-size: var(--font-xs); font-weight: 500; color: var(--color-text-main); }

/* 菜单行 */
.menu-row {
  display: flex; align-items: center; justify-content: space-between;
  padding: var(--spacing-md) 0;
  border-bottom: 1rpx solid var(--color-border);
  transition: opacity 0.2s ease;
}

.menu-row:last-child { border-bottom: none; }
.menu-row:active { opacity: 0.6; }
.menu-row-left { display: flex; align-items: center; }

.menu-icon {
  width: 48rpx; height: 48rpx;
  border-radius: var(--radius-sm);
  display: flex; align-items: center; justify-content: center;
  font-size: var(--font-sm); margin-right: var(--spacing-md);
}

.menu-label {
  font-size: var(--font-base);
  font-weight: 600;
  color: var(--color-text-title);
}

.menu-row-right { display: flex; align-items: center; }
.menu-extra {
  font-size: var(--font-sm);
  color: var(--color-text-sub);
  margin-right: var(--spacing-xs);
  font-weight: 500;
}

.menu-extra.highlight { color: var(--color-primary-dark); }
.menu-chevron { font-size: var(--font-md); font-weight: 300; color: var(--color-text-muted); }

/* 手机号绑定 */
.phone-badge {
  display: inline-flex; align-items: center;
  background: rgba(255,255,255,0.25);
  backdrop-filter: blur(8px);
  padding: 4rpx 14rpx;
  border-radius: var(--radius-capsule);
  font-size: var(--font-xs); font-weight: 500;
  border: 1rpx solid rgba(255,255,255,0.35);
  align-self: flex-start;
  margin-top: 6rpx;
  color: #fff;
}
.phone-icon { margin-right: 4rpx; font-size: var(--font-xs); }

.phone-bind-btn {
  display: inline-flex; align-items: center;
  background: rgba(255,255,255,0.25);
  backdrop-filter: blur(8px);
  padding: 6rpx 18rpx;
  border-radius: var(--radius-capsule);
  font-size: var(--font-xs); font-weight: 600;
  border: 1rpx solid rgba(255,255,255,0.4);
  align-self: flex-start;
  margin-top: 6rpx;
  color: #fff;
  line-height: 1.4;
}
.phone-bind-btn::after { border: none; }

.login-tip {
  margin-top: 6rpx;
  font-size: var(--font-xs);
  color: rgba(255,255,255,0.8);
  line-height: 1.4;
}

.logout-btn {
  margin-top: 22rpx;
  height: 74rpx;
  border-radius: var(--radius-capsule);
  background: rgba(255,255,255,0.2);
  color: #fff;
  border: 1rpx solid rgba(255,255,255,0.35);
  font-size: var(--font-xs);
  font-weight: 600;
}

.logout-btn::after { border: none; }

.guest-showcase-card {
  position: relative;
  overflow: hidden;
  border-radius: var(--radius-xl);
  background:
    radial-gradient(circle at top right, rgba(255,255,255,0.18), transparent 35%),
    linear-gradient(145deg, #F8EFE3, #F3E5D2 55%, #EAD6BF);
  box-shadow: 0 20rpx 48rpx rgba(139,107,74,0.12);
  border: 1rpx solid rgba(139,107,74,0.1);
}

.guest-showcase-glow {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 18% 18%, rgba(255,255,255,0.4), transparent 28%),
    radial-gradient(circle at 82% 82%, rgba(212,165,116,0.18), transparent 26%);
  pointer-events: none;
}

.guest-showcase-content {
  position: relative;
  z-index: 1;
  padding: var(--spacing-lg);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
}

.guest-showcase-badge {
  display: flex;
  align-self: flex-start;
  padding: 8rpx 20rpx;
  border-radius: var(--radius-capsule);
  background: rgba(139,107,74,0.12);
  color: #8B6B4A;
  font-size: var(--font-xs);
  font-weight: 700;
}

.guest-showcase-title {
  font-size: 52rpx;
  font-weight: 700;
  color: var(--color-text-title);
  line-height: 1;
}

.guest-showcase-subtitle {
  font-size: var(--font-sm);
  color: var(--color-text-sub);
  line-height: 1.7;
}

.guest-benefit-list {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.guest-benefit-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-sm) 0;
}

.guest-benefit-icon {
  width: 64rpx;
  height: 64rpx;
  border-radius: 18rpx;
  background: rgba(255,255,255,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-md);
  flex-shrink: 0;
}

.guest-benefit-meta {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.guest-benefit-name {
  font-size: var(--font-base);
  font-weight: 600;
  color: var(--color-text-title);
}

.guest-benefit-desc {
  font-size: var(--font-xs);
  color: var(--color-text-sub);
  line-height: 1.6;
}

.guest-login-btn {
  margin-top: var(--spacing-xs);
  height: 84rpx;
  border-radius: var(--radius-capsule);
  background: linear-gradient(135deg, #D4A574, #B8916B);
  color: #fff;
  font-size: var(--font-base);
  font-weight: 700;
  box-shadow: 0 12rpx 28rpx rgba(184,145,107,0.16);
}

.guest-login-btn::after { border: none; }

.guest-privacy-card {
  margin-top: var(--spacing-xs);
  padding: var(--spacing-md);
  border-radius: var(--radius-lg);
  background: rgba(255,255,255,0.5);
  border: 1rpx solid rgba(139,107,74,0.12);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
}

.guest-privacy-loading {
  opacity: 0.88;
}

.guest-privacy-title {
  font-size: var(--font-sm);
  font-weight: 700;
  color: var(--color-text-title);
}

.guest-privacy-desc {
  font-size: var(--font-xs);
  color: var(--color-text-sub);
  line-height: 1.7;
}

.guest-privacy-actions {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  margin-top: var(--spacing-xs);
}

.guest-privacy-link-btn,
.guest-privacy-agree-btn {
  width: 100%;
  min-height: 76rpx;
  border-radius: var(--radius-capsule);
  font-size: var(--font-sm);
  font-weight: 700;
}

.guest-privacy-link-btn {
  background: rgba(255,255,255,0.78);
  color: #8B6B4A;
  border: 1rpx solid rgba(139,107,74,0.18);
}

.guest-privacy-agree-btn {
  background: linear-gradient(135deg, #D4A574, #B8916B);
  color: #fff;
  box-shadow: 0 10rpx 24rpx rgba(184,145,107,0.12);
}

.guest-privacy-link-btn::after,
.guest-privacy-agree-btn::after {
  border: none;
}

/* 页脚 */
.page-footer {
  text-align: center; font-size: var(--font-xs);
  padding: var(--spacing-xl) 0 var(--spacing-lg);
  color: var(--color-text-muted);
  letter-spacing: 1rpx;
}
